[build]
builder = "DOCKERFILE"
dockerfilePath = "services/homeserver/Dockerfile"
watchPatterns = ["services/homeserver/**", "shared/**"]

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 120
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
